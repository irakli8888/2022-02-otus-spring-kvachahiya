spring:
  data:
    mongodb:
      port: 27017
      database: library
      username: mongo
      password: mongo
      host: localhost
      authentication-database: admin
  main:
    allow-circular-references: true

mongock:
  runner-type: "InitializingBean"
  change-logs-scan-package:
    - ru.otus.springdatamongodb.migrations